# Only needed when running the backend outside Docker (npm run dev).
# With docker compose, use the root .env instead.
PORT=4000
DATABASE_URL=postgres://calenmate:calenmate@localhost:5432/calenmate
JWT_SECRET=change-me-to-a-long-random-string
COOKIE_SECURE=false
SEED_DEMO_DATA=true
FRONTEND_URL=http://localhost:3000
# https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
